// Optional override file.
// The app already includes the default Supabase URL and anon public key in db.js.
// Use this only if you want to point the app to another Supabase project later.

window.SUPABASE_URL = "https://YOUR_PROJECT_ID.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR_ANON_PUBLIC_KEY";
